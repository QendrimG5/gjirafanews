// This file runs after Jest is initialized but before any test file.
// It imports @testing-library/jest-dom which adds custom DOM matchers
// like .toBeInTheDocument(), .toHaveTextContent(), .toBeVisible(), etc.
import "@testing-library/jest-dom";
